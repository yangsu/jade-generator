- var items = [1,2,3]
ul
  - items.forEach(function(item){
    li= item
  - })
- var items = [1,2,3]
ul
  for item, i in items
    li(class='item-' + (i) + '')= item
ul
  for item, i in items
    li= item
ul
  for $item, $index in items
    li= $item
- var nums = [1, 2, 3]
- var letters = ['a', 'b', 'c']
ul
  for l, $index in letters
    for n, $index in nums
      li
        | #{n}: #{l}
- var count = 1
- var counter = function() { return [count++, count++, count++] }
ul
  for n, $index in counter()
    li
      | #{n}